# premake output
bin/
bin-int/
*.make
Makefile
!scripts/**
compile_commands.json

# bookkeeping — not repo content
design/
PLAN.md
CHANGELOG.md

# per-user editor state
imgui.ini
.cache/
.idea/
.vscode/
*.user

# compiled shader cache
**/assets/cache/

# generated by scripts/GenWaylandProtocols.sh and scripts/EmbedShaders.sh
VAE/vendor-generated/

# written by premake5: what this engine build links, for the SDK to read
sdk/config.lua

# verification captures
shots/

# a stray project written at the root by an older Studio; projects live under the user's
# projects root now (Documents/VAE, or $VAE_PROJECTS), never in the checkout
*.vaescreen
*.vaeproj
*.vaecomp
*.store.json
project.lua
project.cpp
project.so
studio-recent.txt
